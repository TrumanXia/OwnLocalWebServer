0.0.1-SNAPSHOT -- 说明

1. 创建Maven simple project
2. 搭建本地服务器，可以通过运行jar来进行监听本地请求(localhost:6666/hello.html)
3. 请求支持范围（持续添加中）： 
	
-- 未完待续
抽象出实体类，但未实现持久化

-- bug
试过 IE 和 chrome 使用 localhost:6666/hello.html 访问，chrome没有不发出HTTP请求